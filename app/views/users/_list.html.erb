
      <h2>User info</h2>
      <%= form_with model: user do |f| %>
      <%= image_tag user.get_profile_image(100,100) %>

  <table class="table table">
    <tbody>
      <tr>
        <td>name</td>
        <td><%= user.name %></td>
      </tr>
      <tr>
        <td>introduction</td>
        <td><%= user.introduction %></td>
      </tr>
    </tbody>
    </table>

        <button class="navbar-toggler">
          <%= link_to edit_user_path(user), class: 'btn btn-outline-secondary' do %>
            <span class="fa-solid fa-user-gear"></span>
          <% end %>
        </button>


<% end %>

      <%= form_with model: book do |f| %>
        <h2>New book</h2>

        <h5>Title</h5>
        <%= f.text_field :title %>

        <h5>Opinion</h5>
        <%= f.text_area :body %>

        <h5><%= f.submit 'Create book', class: 'btn btn-success' %></h5>

      <% end %>



